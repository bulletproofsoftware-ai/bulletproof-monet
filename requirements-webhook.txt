# Runtime dependencies for api/monet_webhook.py (the Telegram webhook receiver)
# and for the tests that import fastapi.testclient.
#
# install.sh step 7 installs these into $MONET_HOME/webhook-venv, which is the
# path src/monet-webhook.service.template runs uvicorn from.
#
# Pins verified to install together in a clean venv on CPython 3.11 and 3.12.
fastapi==0.140.0
uvicorn[standard]==0.52.0
httpx==0.28.1
